
import Layout from '@/components/Layout';
import MultipliersListPage from '@/components/MultipliersListPage';

const Multipliers = () => {
  return (
    <Layout>
      <MultipliersListPage />
    </Layout>
  );
};

export default Multipliers;
